import admin from './admin';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = () => {
    const menuItems = {
        items: [admin]
    };
    return menuItems;
};

export default menuItems;
